# X digest supplemental searches — runs in addition to the For You feed pull.
#
# Why: For You is X's algorithmic feed, so it only shows what X *thinks* you
# want. These searches catch high-engagement content in your interest topics
# that For You might skip — especially when you haven't engaged with a topic
# recently and X stops showing it.
#
# Format: name|bird-search-query|max_results
#  - name: human-readable label (logged but not used in scoring)
#  - query: anything `bird search` accepts (see bird CLI). Combine X
#           search operators: from:user, OR, min_faves:N, since:YYYY-MM-DD
#  - max_results: cap on how many to pull from this search (3-5 is sane)
#
# Lines starting with # and blank lines are ignored.
# Total candidates after merge ≈ For You (~30) + sum(max_results) here.

# --- Always-include people ---
karpathy|from:karpathy|3
bcherny|from:bcherny min_faves:50|3

# --- Claude Code / Kiro CLI ---
claude_code|claude code min_faves:500|5
kiro_cli|kiro-cli OR "kiro one" min_faves:200|5
# Official tool-vendor accounts (added 2026-07-12): Brandon's daily toolchain is
# Kiro + Claude Code + Codex — releases/changelogs shouldn't depend on the algo
# feed. Keyword-gated to product news; account posts are low-volume anyway.
tool_official|from:kirodotdev OR from:AnthropicAI OR from:claudeai (Kiro OR Claude OR Code OR release OR update OR launch OR skill OR agent)|4

# --- OpenAI Codex (coding agent / CLI) — added 2026-07-12 per Brandon ---
# Release/changelog trackers (dedicated accounts, always on-topic).
codex_releases|from:CodexReleases OR from:Codex_Changelog|3
# Popular community posts — phrase-anchored to dodge manuscript / Codex
# Alimentarius / game-"Codex" noise. min_faves:300 keeps it to the popular ones.
codex_popular|("openai codex" OR "codex cli" OR "codex-cli") min_faves:300|5

# --- Agent skills / SKILL.md ecosystem (TOP INTEREST — boosted 2026-07-14 per
#     Brandon: "I want skills to show up more". Floor 200->100, cap 5->8, broader
#     terms, plus a dedicated skill-authors account line.) ---
agent_skills|"SKILL.md" OR "agent skill" OR "agent skills" OR "npx skills" OR "claude skill" OR ClawHub min_faves:100|8
agent_skills_authors|from:obra OR from:nutlope OR from:mattpocock OR from:kepano OR from:nichochar (skill OR SKILL.md OR "npx skills")|4

# --- Agent workflow orchestration / multi-agent (added 2026-07-14 per Brandon:
#     orchestration IS wanted; it was only the "loop" terminology to avoid.
#     Query deliberately omits the word "loop".) ---
agent_orchestration|"multi-agent" OR "multi agent" OR "agent orchestration" OR "agent workflow" (framework OR orchestration OR workflow OR handoff OR planner OR coordinate OR agents) min_faves:100|4

# --- Eval-driven development / testing agents (Brandon filed openclaw-v3w;
#     expanded 2026-07-11 per Brandon: "more of that around testing agents";
#     widened 2026-07-12 — Brandon is BUILDING an agent-eval framework at work
#     (DeepEval-style metrics), so this is his active project, not just intel.
#     Floors lowered 100→50 / 20→10: eval posts are niche, good ones land
#     at 20-80 likes, the old floors starved the digest's thinnest pipe.) ---
evals|deepeval OR "agent eval" OR "agent evals" OR "agent evaluation" OR "eval harness" OR "LLM-as-a-judge" OR "LLM as a verifier" OR "agent benchmark" OR "Terminal-Bench" OR "SWE-bench" OR "agent testing" min_faves:50|6
# Eval / agent-testing authors — teachers + researchers, account+keyword so we
# pull their eval/testing posts specifically. jackyk02 + Azaliamirh authored the
# LLM-as-a-Verifier work Brandon flagged (finances-tyf) on 2026-07-11.
eval_authors|from:HamelHusain OR from:eugeneyan OR from:sh_reya OR from:lateinteraction OR from:jackyk02 OR from:Azaliamirh (eval OR agent OR benchmark OR verifier OR test OR judge OR rubric OR metric) min_faves:10|5
# Eval-platform principals — what DeepEval-adjacent tools are shipping.
# ankrgyl=Braintrust founder, arizeai+aparnadhinak=Arize/Phoenix (evals+observability).
# NOTE: @deepeval + @confident_ai corp accounts verified DEAD 2026-07-12 (0 posts
# in 30d) — the founders don't post either; DeepEval intel comes via the keyword
# line above. Re-check quarterly.
eval_frameworks|from:ankrgyl OR from:arizeai OR from:aparnadhinak (eval OR judge OR metric OR agent OR benchmark OR observability)|4

# --- AWS surface area Brandon works on (ProServe Connect engineer — this is
#     the DAY JOB block; expanded 2026-07-12) ---
# Connect is niche: good posts land at 5-20 likes, so NO min_faves — the
# script-side default floor (3) + LLM filter handle noise. Covers Connect
# Health, Contact Lens, Q in Connect, Cases, CCaaS moves.
amazon_connect|"amazon connect" OR "amazon q in connect" OR ("contact lens" (CCaaS OR "contact center" OR agent))|4
# AWS official firehose, keyword-gated to agentic/Connect/serverless topics so
# it doesn't flood the merge. Restored 2026-07-12: the For You rewrite dropped
# v3's official-accounts line — launch news shouldn't depend on the algo feed.
aws_official|from:awscloud OR from:awsdevelopers OR from:jeffbarr (Connect OR Bedrock OR agent OR AI OR Lambda OR launch OR GA)|4
bedrock_agentcore|bedrock agentcore OR "agent core" min_faves:50|3
aws_ai|AWS bedrock OR strands min_faves:100|3

# --- MCP / tool integrations (de-emphasized 2026-07-14 per Brandon: "don't
#     emphasize MCP too much" — floor 200->500, cap 5->2, only the biggest news) ---
mcp|"mcp server" OR "mcp tool" min_faves:500|2

# --- Voice / TTS / conversational AI ---
# Rewritten 2026-07-12: old query (elevenlabs OR vapi OR "voice agent"
# min_faves:200) went 12 pulled / 0 surfaced — pure vendor marketing. Anchor on
# build/ship substance instead; relevant to Brandon via Connect voice channels.
voice_tts|("voice agent" OR "voice AI" OR "speech to speech") (built OR building OR shipped OR latency OR pipeline OR benchmark OR open source) min_faves:100|3

# --- AI for productivity (Gmail/Calendar/Drive automation) ---
ai_productivity|"google workspace" agent OR "gmail" agent min_faves:200|3

# --- Real Estate x AI (added 2026-07-15 per Brandon: nothing RE-AI was ever
#     surfacing because it lived only in the unused x-digest-topics.txt, never
#     in this file. Brandon has a presentation next Fri on using AI in real
#     estate and wants inspiration.) Reuses the hand-vetted queries from the
#     old topics file. Tier 1 = verified voices + portals/media; Tier 2 =
#     popular practitioner buzz anchored on realtor/agent/broker terms (NOT
#     bare "real estate", which is a macro-finance magnet). The LLM relevance
#     filter strips remaining off-topic collisions. ---
# Tuned 2026-07-18 (Brandon): added alexgroberman (RE + AI Search Optimization/AEO)
# and recap_david (AI listing->cinematic video) — both verified active + posting
# RE-AI content in a live pull; the fixed-9 list was missing the practitioner
# voices actually driving the conversation. Broadened the community line below too.
real_estate_ai_voices|from:RyanSerhant OR from:tomferry OR from:robhahn OR from:Inman OR from:HousingWire OR from:ByronLazine OR from:TheBrokeAgent OR from:zillow OR from:Redfin OR from:alexgroberman OR from:recap_david (AI OR ChatGPT OR "artificial intelligence" OR automation OR "machine learning")|6
real_estate_ai_community|(realtor OR "real estate agent" OR "listing agent" OR "real estate broker" OR "real estate marketing" OR "home staging" OR "virtual staging" OR "AI staging" OR "listing description" OR "property video" OR "real estate leads") (AI OR ChatGPT OR "artificial intelligence" OR automation OR "AI agent") min_faves:50 lang:en -filter:replies -filter:retweets -crypto -web3 -giveaway -stock -shares -dividend -bubble|6

# --- SpaceX / Tesla ---
# Official accounts get their own line (no min_faves) so they can't be crowded
# out by viral keyword matches when max_results is tight.
spacex_official|from:SpaceX|5
tesla_official|from:Tesla|5
# Topic-only catches viral 3rd-party posts (Starship updates, Cybercab leaks, FSD demos).
spacex_topics|starship OR "raptor engine" OR "super heavy" min_faves:1000|3
tesla_topics|cybercab OR optimus OR robotaxi OR FSD min_faves:1000|3
elonmusk_top|from:elonmusk min_faves:10000|2
